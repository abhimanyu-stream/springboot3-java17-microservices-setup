package com.stream.product.catalogue.wishlist.repository;

import com.stream.product.catalogue.wishlist.model.BeautyAndPersonalCareProduct;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BeautyAndPersonalCareProductRepository extends JpaRepository<BeautyAndPersonalCareProduct, Long> {
}
